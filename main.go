// Marvish Chandra

package CalculateMyUtilities

import "fmt"

func myInternet(n,c);
hundredmbps = (n / c) * 100
twohundred_mbps = (n / c) * 200
threehundred_mbps = (n / c) * 300
thousand_mbps = (n / c) * 1000

func myElectricity(avgkwHours,avgcentskwHours):
totalElectricity = (avgkwHours / avgcentskwHours) / 100

func myWater(myUsage,areaCost):
totalWater = myUsage * areaCost

func cellularPlan(p,c):
totalCellular = p * c

func garbageDisposal(minimum,standard,oversize,c):
entryPlan = minimum * c
midPlan = standard * c
premiumPlan = oversize * c
